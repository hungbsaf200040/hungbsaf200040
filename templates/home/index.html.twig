{% extends 'base.html.twig' %}

{% block body %}
    <div class="bs-example" data-example-id="striped-table">
{#        <table class="table table-striped">#}
{#            <thead>#}
{#            <tr>#}
{#                <th>#</th>#}
{#                <th>Category</th>#}

            </tr>
            </thead>
            <tbody>
            {#          .  {% for category in categories %}#}
            {#                <tr>#}
            {#                    <th scope="row">{{ category.id }}</th>#}
            {#                    <td><a href="{{path('productByCat',{'id':category.id})}} ">{{ category.catname }}</a></td>#}

            {#                    <td>#}
            {#                        #}{#                        <a href="{{ path('todo_details', {'id': product.id}) }} " class='btn btn-success'>View</a>#}
            {#                        #}{#                        <a href="{{ path('todo_edit', {'id': product.id}) }}" class='btn btn-default'>Edit</a>#}
            {#                        #}{#                        <a href="{{ path('todo_delete', {'id': product.id}) }}" class='btn btn-danger'>Delete</a>#}
            {#                        <a href="{{path('productByCat',{'id':category.id})}} " class='btn btn-success'>View</a>#}
            {#                        <a href="#" class='btn btn-default'>Edit</a>#}
            {#                        <a href="{{ path('product_delete', {'id': category.id}) }} "class='btn btn-danger'>Delete</a>#}
            {#                    </td>#}
            {#                </tr>#}
            {#           . {% endfor %}#}
            </tbody>
        </table>
    </div>
    <div class="bs-example" data-example-id="striped-table">
        <table class="table table-striped">
            <thead>
            <tr>
                <th>#</th>
                <th>Product Name</th>
                <th>description</th>
                <th>Img</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for product in products %}
                <tr>
                    <th scope="row">{{ product.id }}</th>
                    <td>{{ product.productName }}</td>
                    <td>{{ product.description }}</td>
                    <td> <img src="{{ asset('upload/imageProduct/' ~ product.productimg) }}" style="width: 148px;height: 148px"/><td>
                        {#                    <td> <img src="{{ asset('upload/imageProduct/' ~ product.ProductImages) }}" style="width: 148px;height: 148px"/><td>>#}
                    <td>
                        {#                    .    <a href="{{ path('todo_details', {'id': product.id}) }} " class='btn btn-success'>View</a>#}
                        {#                        <a href="{{ path('todo_edit', {'id': product.id}) }}" class='btn btn-default'>Edit</a>#}
                        {#                        <a href="{{ path('todo_delete', {'id': product.id}) }}" class='btn btn-danger'>Delete</a>#}
                        <a href="{{path('product_details',{'id':product.id})}} " class='btn btn-success'>View</a>
                        {#                        <a href="{{path('product_edit',{'id':product.id})}}" class='btn btn-default'><i class="fa fa-pencil-square-o" style="color: black; font-size: 20px">edit</i></a>#}
                        {#                        <a href="{{path('product_delete',{'id':product.id})}} " class='btn btn-danger'>Delete</a>#}
                        {#                       <a href="{{ path('product_delete', {'id': product.id}) }} "class='btn btn-danger'>Delete</a>#}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

